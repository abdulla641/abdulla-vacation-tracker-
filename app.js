let expenses=JSON.parse(localStorage.getItem('expenses'))||[];let wallet=JSON.parse(localStorage.getItem('wallet'))||[];function save(){localStorage.setItem('expenses',JSON.stringify(expenses));localStorage.setItem('wallet',JSON.stringify(wallet))}function addMoney(){let a=Number(moneyAmount.value);if(!a)return;wallet.push({amount:a,note:moneyNote.value,date:new Date()});save();render()}function addExpense(){let a=Number(amount.value);if(!a)return;expenses.push({amount:a,note:note.value,date:new Date()});save();render()}function render(){let av=wallet.reduce((a,b)=>a+b.amount,0),sp=expenses.reduce((a,b)=>a+b.amount,0);moneyAvailable.innerHTML='₹'+av;totalSpent.innerHTML='₹'+sp;remainingMoney.innerHTML='₹'+(av-sp);transactionCount.innerHTML=wallet.length+expenses.length;history.innerHTML=[...wallet,...expenses].map(x=>'₹'+x.amount+'<br>📅 '+new Date(x.date).toLocaleString()).join('<hr>');tripDay.innerHTML='Kerala Vacation Tracker'}function exportData(){alert('Backup ready')}render();